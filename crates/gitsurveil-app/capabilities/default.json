{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for both webviews. Deliberately minimal: they render lists and ask the Rust side to do everything else, so they need no filesystem, shell, or network access of their own.",
  "windows": [
    "popover",
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "opener:default",
    "positioner:default",
    "dialog:default"
  ]
}
