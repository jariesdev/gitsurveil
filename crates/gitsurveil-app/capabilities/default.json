{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the notifications popover. Deliberately minimal: the webview renders a list and asks the Rust side to do everything else, so it needs no filesystem, shell, or network access of its own.",
  "windows": ["popover"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "opener:default",
    "positioner:default"
  ]
}
